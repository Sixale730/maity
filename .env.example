# ================================
# Maity Platform - Environment Variables
# ================================
#
# Copy this file to .env.local for local development
# Never commit .env.local to git!
#
# For production, set these in your deployment platform (Vercel, etc.)

# ================================
# REQUIRED - Supabase Configuration
# ================================

# Supabase Project URL
# Example: https://your-project.supabase.co
VITE_SUPABASE_URL=https://nhlrtflkxoojvhbyocet.supabase.co

# Supabase Anonymous (Public) Key
# This key is safe to use in client-side code
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ================================
# REQUIRED - Application URLs
# ================================

# API Base URL
# Development: http://localhost:3000
# Production: https://www.maity.com.mx
VITE_API_URL=http://localhost:3000

# Application Base URL
# Development: http://localhost:5173
# Production: https://www.maity.com.mx
VITE_APP_URL=http://localhost:5173

# Canonical URL (for SEO and redirects)
# Optional - defaults to VITE_APP_URL if not set
VITE_CANONICAL_URL=https://www.maity.com.mx

# ================================
# OPTIONAL - Third-Party Integrations
# ================================

# ElevenLabs Voice AI Configuration (optional)
# Only needed for voice practice features
VITE_ELEVENLABS_API_KEY_TEST=
VITE_ELEVENLABS_AGENT_ID_TEST=
VITE_ELEVENLABS_INTERVIEW_AGENT_ID=agent_4901k7q65b35emqs26mq22kqaq7g
VITE_ELEVENLABS_TECH_WEEK_AGENT_ID=agent_3301k8nsyp5jeqfb84n0y0p5jd2g

# OpenAI Configuration (for evaluation processing)
# Model to use for roleplay evaluations
VITE_OPENAI_MODEL=gpt-4o-mini

# Tally Forms Integration (optional)
# Only needed for registration forms
VITE_TALLY_FORM_URL=
VITE_TALLY_WEBHOOK_SECRET=

# ================================
# OPTIONAL - Security & CORS
# ================================

# CORS Allowed Origins (comma-separated)
# Example: https://www.maity.com.mx,https://app.maity.com.mx
VITE_CORS_ORIGINS=

# Cookie Domain (for invite system)
# Example: .maity.com.mx
VITE_COOKIE_DOMAIN=

# ================================
# SERVER-SIDE ONLY (API endpoints)
# ================================
# These are NOT prefixed with VITE_ and won't be exposed to the client

# Supabase Service Role Key (KEEP SECRET!)
# Only use in API endpoints, never in client code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# OpenAI API Key (KEEP SECRET!)
# Used for evaluation processing in API endpoints
OPENAI_API_KEY=sk-your-openai-api-key-here

# Tally Webhook Secret (webhook validation)
TALLY_WEBHOOK_SECRET=

# ================================
# NOTES
# ================================
#
# 1. Variables prefixed with VITE_ are exposed to the browser
#    - Safe to use for public API keys (anon key)
#    - Never use for secrets (service role key)
#
# 2. Variables without VITE_ prefix are server-side only
#    - Only accessible in API endpoints (api/*.js)
#    - Use these for service role keys and secrets
#
# 3. For local development:
#    - Copy this file to .env.local
#    - Fill in your development values
#    - .env.local is gitignored automatically
#
# 4. For production deployment:
#    - Set these in Vercel/deployment platform
#    - Use separate values for staging/production
#    - Never commit real secrets to git
